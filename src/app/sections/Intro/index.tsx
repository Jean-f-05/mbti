import IconsBar from '@/app/components/IconsBar'
import styles from "./styles.module.scss"

const Intro = () => {
    return (
        <section className={styles.intro__wrapper}>
            <IconsBar />
        </section>
    )

}

export default Intro